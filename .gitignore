# Secrets: never commit. Git history is permanent; a leaked key must be rotated.
*config.json
config.json
.env
.env.*
*.pem
*.key
*.p12
*.pfx
token.json
*token.json
*credentials*.json
id_rsa
id_rsa.*

# Local state / scratch. The empty per-install scaffold ships in copilot-pack/user/;
# only a stray repo-root user/ (e.g. a real filled-in install) is ignored.
/user/
tmp/
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# library.py runtime cache (per-install)
.library-cache.json
